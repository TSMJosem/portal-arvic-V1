# Esta es una plantilla de las variables de entorno

# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/nombre-base-datos?retryWrites=true&w=majority

# Puerto del servidor local (solo para desarrollo)
PORT=3000

# Entorno de ejecución
NODE_ENV=development

# JWT Secret (genera uno nuevo usando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# ================================
# NOTAS IMPORTANTES:
# ================================
# 1. NUNCA subas este archivo con valores reales a Git
# 2. Copia este archivo como .env y llena los valores
# 3. Para producción (Vercel), configura estas variables en el dashboard
# 4. El JWT_SECRET debe ser diferente en desarrollo y producción
